version: 2.1
orbs:
  node: circleci/node@4.5.1
jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - run: git config user.email "jwong123@binghamton.edu"
      - run: git config user.name "Jonathan Wong"
      - run: yarn install
      - run: yarn build
      - run: yarn push
workflows:
  build-and-test:
    jobs:
      - build-and-test:
        filters:
          branches:
            only: develop
